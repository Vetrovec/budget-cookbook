import Layout from '../components/Layout';
import { Typography } from '@mui/material';

export function Root() {
	return (
		<Layout>
			<Typography variant="h3">Hello from Budget cookbook!</Typography>
		</Layout>
	);
}
